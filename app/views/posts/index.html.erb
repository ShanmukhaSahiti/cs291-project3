<div class="container" style="margin-top: 1rem;">
  
  <p class="lead"> Welcome to CS291's twitter! Browse through all the posts available or go ahead and create a new post of yours!</p> 
  <%= render "posts/form", post: @post %>
  <br/>
  
  <h4> All Posts </h4>
    <p class="lead"> Click on the comments link to add your comments to an existing post.  </p>
  
  <% @posts.each do |post| %>
    <div class="container">
      <small id="subtitle" class="form-text text-muted"><%=post.author%> at <%= post.updated_at%></small>
      <p><%= post.body %>
        <br/>
        <%= link_to post.comments.length.to_s + " Comments", post%>
      </p>
    </div>
  <% end %>

<%# <%= link_to "New Post", new_post_path %>
</div>